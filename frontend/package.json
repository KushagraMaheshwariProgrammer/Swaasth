{
  "name": "medbill-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:firebase": "cp google-services.json android/app/google-services.json",
    "build:dev": "npm run sync:firebase && vite build --mode development && npx cap sync android",
    "build:release": "node scripts/verify-release-env.mjs && npm run sync:firebase && vite build && npx cap sync android",
    "build": "npm run build:dev",
    "preview": "vite preview",
    "cap:sync": "npm run sync:firebase && npx cap sync android",
    "sync:android": "npm run cap:sync"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.3.0",
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "firebase": "^12.14.0",
    "framer-motion": "^12.38.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.14.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "@vitejs/plugin-react": "^5.1.0",
    "vite": "^7.1.12"
  }
}
